[project]
name = "evalforge"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
evalforge = "evalforge.cli:app"

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.setuptools.packages.find]
include = ["evalforge*"]
