{"code": "def fib(n):\n    if n <= 1:\n        return n\n    return fib(n-1) + fib(n-2)", "explanation": "This function recursively computes the nth Fibonacci number by summing the two preceding values until it reaches the base cases 0 or 1."}
{"code": "squares = [x**2 for x in range(10) if x % 2 == 0]", "explanation": "It builds a list of squared even numbers from 0 up to 8 using a list comprehension that filters out odd integers."}
{"code": "def timer(fn):\n    def wrapper(*args, **kwargs):\n        print('calling')\n        return fn(*args, **kwargs)\n    return wrapper", "explanation": "This pattern defines a decorator factory that wraps another function to run side effects before delegating to the original callable."}
{"code": "function binarySearch(arr, target) {\n  let lo = 0, hi = arr.length - 1;\n  while (lo <= hi) {\n    const mid = (lo + hi) >> 1;\n    if (arr[mid] === target) return mid;\n    if (arr[mid] < target) lo = mid + 1;\n    else hi = mid - 1;\n  }\n  return -1;\n}", "explanation": "It searches a sorted array for a value by repeatedly halving the search interval using middle-index comparisons until the target is found or the range empties."}
{"code": "class Point:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y", "explanation": "This class constructor stores two coordinates on each new instance so other methods can access them as attributes."}
{"code": "const sum = (a, b) => a + b;", "explanation": "This arrow function returns the sum of its two parameters in JavaScript."}
{"code": "try:\n    data = int(input())\nexcept ValueError:\n    data = 0", "explanation": "It reads user input as an integer and falls back to zero when the text cannot be parsed as a number."}
{"code": "for i, name in enumerate(names):\n    print(i, name)", "explanation": "This loop walks a sequence of names, printing each index paired with its corresponding list element."}
{"code": "fetch(url).then(r => r.json()).then(console.log);", "explanation": "It issues an HTTP request, parses the response body as JSON once it completes, then logs the resulting object."}
{"code": "match x:\n    case 0:\n        print('zero')\n    case _:\n        print('other')", "explanation": "This structural pattern match branches on the value of x and runs the first block for zero or the default branch otherwise."}
